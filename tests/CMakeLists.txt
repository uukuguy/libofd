PROJECT(libofd)

AUX_SOURCE_DIRECTORY(. SRC_LIST)
ADD_EXECUTABLE(ofdtest ${SRC_LIST})

# -------- Freetype --------
FIND_PACKAGE(Poppler REQUIRED)
INCLUDE_DIRECTORIES(${POPPLER_INCLUDE_DIRS})

TARGET_LINK_LIBRARIES(ofdtest ofd ${POPPLER_LIBRARY})
